# aws-cowrie-splunk-honeypot
Cloud-hosted SSH honeypot built with AWS, Ubuntu, Cowrie, and Splunk Enterprise to capture, forward, and analyze real-world SSH attack activity using the HTTP Event Collector (HEC).
